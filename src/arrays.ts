// two ways to initialize arrays
const numbers: number[] = [1, 2, 3, 4]
const names: Array<string> = ["Alice", "Bob", "Charlie"] // older method, not used much

const items: string[] = []
items.push("keyboard")

const numsList: number[] = []
numsList.push(20)
// numsList.push(true) // ERROR

// Multidimensional Arrays
const matrix: number[][] = [
    [1, 2],
    [3, 4],
]
